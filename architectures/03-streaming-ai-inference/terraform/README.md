# Terraform skeleton — Streaming AI inference

Creates the streaming Lambda + Function URL, DynamoDB memory table, S3 + CloudFront for the static UI. Cognito is referenced via variable — bring your own user pool.
