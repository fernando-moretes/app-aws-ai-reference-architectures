# Terraform skeleton — Multi-agent orchestration

Creates Step Functions state machine, DynamoDB tables, S3 bucket and IAM roles. Bedrock Agents are referenced by ID and provisioned separately while iterating on prompts.
